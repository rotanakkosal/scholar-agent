# Normalize line endings: LF in the repo, auto on checkout.
* text=auto eol=lf

# Treat these as binary (no EOL conversion / no diff noise).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.ico binary
