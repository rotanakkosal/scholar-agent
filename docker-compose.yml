services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: scholar-agent
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
